{
  "name": "tinfoil",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "test": "npm run test -ws --if-present",
    "clean": "npm run clean -ws --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
